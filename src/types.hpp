#pragma once
#include <cstdint>
#include <SFML/Graphics.hpp>

using u8 = sf::Uint8;
using u16 = sf::Uint16;
using u32 = sf::Uint32;
using u64 = sf::Uint64;

using i8 = sf::Int8;
using i16 = sf::Int16;
using i32 = sf::Int32;
using i64 = sf::Int64;

using f32 = float;
using f64 = double;

using vec2f = sf::Vector2f;
